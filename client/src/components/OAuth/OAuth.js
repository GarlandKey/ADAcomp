import './OAuth.css';
import {Link} from "react-router-dom";

function OAuth() {
  return (
    <div className="OAuth">
      <Link to="/Point">
        <button>Demo</button>
      </Link>
    </div>
  );
}

export default OAuth;
